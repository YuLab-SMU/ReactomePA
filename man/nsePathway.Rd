% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nsea.R
\name{nsePathway}
\alias{nsePathway}
\title{Network-based Reactome pathway enrichment analysis}
\usage{
nsePathway(
  geneList,
  organism = "human",
  mode = c("evidence", "signed"),
  network = NULL,
  restart_prob = 0.5,
  specific_weight = FALSE,
  minGSSize = 10,
  maxGSSize = 500,
  threshold = 1e-09,
  maxIter = 100,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{geneList}{A named numeric vector of gene-level scores (e.g. log2FC).
Names must be Entrez Gene IDs.}

\item{organism}{Supported organism. Default is "human".}

\item{mode}{Character, either "evidence" (default) or "signed".
In "evidence" mode, all seeds are propagated once.
In "signed" mode, positive and negative seeds are propagated
separately and the difference is taken.}

\item{network}{Optional pre-built network from `prepareReactomeNetwork()`.
If NULL, the network is auto-built and cached.}

\item{restart_prob}{Restart probability for RWR (default: 0.5).}

\item{specific_weight}{Logical, whether to apply gene specificity
weighting (TF-IDF style). Default is FALSE.}

\item{minGSSize}{Minimal size of each gene set for analyzing (default: 10).}

\item{maxGSSize}{Maximal size of each gene set for analyzing (default: 500).}

\item{threshold}{Convergence threshold for RWR (default: 1e-9).}

\item{maxIter}{Maximal number of RWR iterations (default: 100).}

\item{verbose}{Print progress messages (default: TRUE).}

\item{...}{Other arguments passed to `enrichit::gsea_gson()`.}
}
\value{
A `nseaResult` object (inherits from `gseaResult`).
}
\description{
`nsePathway()` performs network-based set enrichment analysis (NSEA)
on Reactome pathways. Instead of using raw gene-level scores directly,
scores are propagated across the Reactome reaction graph via Random
Walk with Restart (RWR), then GSEA is run on the diffused scores.
This captures pathway-level signals that may be diluted in individual
gene-level statistics.
}
\details{
The Reactome reaction network is automatically built and cached on
first call. Users can pre-build and pass a custom network via the
`network` parameter.
}
\examples{
\dontrun{
  data(geneList, package = "DOSE")
  nsea_res <- nsePathway(geneList)
  head(nsea_res)
}
}
\seealso{
[prepareReactomeNetwork()], [enrichit::nsea()]
}
